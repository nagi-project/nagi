version: 2

sources:
  - name: raw
    schema: main
    tables:
      - name: raw_customers
      - name: raw_orders
        columns:
          - name: order_id
            tests:
              - not_null
      - name: raw_products
        tags:
          - product
