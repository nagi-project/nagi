{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Spec for `kind: Origin`. References an external project (e.g. dbt) to auto-generate Assets.",
  "oneOf": [
    {
      "properties": {
        "connection": {
          "description": "Connection resource name for auto-generated Sources.",
          "type": "string"
        },
        "defaultSync": {
          "default": null,
          "description": "Name of the Sync resource applied to all auto-generated Assets unless overridden.",
          "type": [
            "string",
            "null"
          ]
        },
        "projectDir": {
          "description": "Local path to the dbt project directory (relative or absolute).",
          "type": "string"
        },
        "type": {
          "enum": [
            "DBT"
          ],
          "type": "string"
        }
      },
      "required": [
        "connection",
        "projectDir",
        "type"
      ],
      "type": "object"
    }
  ],
  "title": "OriginSpec"
}